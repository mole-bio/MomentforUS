import React from "react";
import AppLayout from "../components/AppLayout";

const Home = () => {
  return (
    <AppLayout>{/* AppLayout에 들어있는 애들이 children이 된다 */}</AppLayout>
  );
};

export default Home;
